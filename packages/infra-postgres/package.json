{
  "name": "@notification-system/infra-postgres",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "postinstall": "prisma generate",
    "prisma:generate": "prisma generate",
    "prisma:validate": "prisma validate",
    "prisma:migrate": "prisma migrate dev",
    "build": "prisma generate && tsc -b",
    "clean": "tsc -b --clean",
    "test": "node --test \"dist/**/*.test.js\"",
    "smoke-test": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "@notification-system/shared-kernel": "workspace:*",
    "@notification-system/domain-identity": "workspace:*",
    "@notification-system/domain-preferences": "workspace:*",
    "@notification-system/domain-templates": "workspace:*",
    "@notification-system/domain-notification": "workspace:*",
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0"
  }
}
