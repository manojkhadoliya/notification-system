// Every other file in this package imports the generated Prisma client
// through here, not `@prisma/client` directly — see schema.prisma's
// generator block for why (a real Prisma 5.x package-exports gotcha, not
// stylistic preference). If Prisma's export strategy changes again later,
// this is the one file that needs to change.
export * from "../generated/prisma-client/index.js";
